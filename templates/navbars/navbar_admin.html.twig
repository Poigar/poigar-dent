<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">

  <a class="navbar-brand" href="#">Poigar Dentist</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">

    <ul class="navbar-nav ml-auto">

      <li class="nav-item">
        <a class="nav-link" href="{{ path('schedule') }}"><i class="fa fa-sign-out"></i> Schedule</a>
      </li>

      {% if(app.session.get('user_permission')==1) %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('employees') }}"><i class="fa fa-user"></i> Employees</a>
        </li>
      {% endif %}

      <li class="nav-item">
        <a class="nav-link" href="{{ path('logout_action') }}"><i class="fa fa-sign-out"></i> Log out</a>
      </li>

    </ul>

  </div>
</nav>